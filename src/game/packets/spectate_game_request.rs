use crate::game::enums;
use crate::models;
use crate::net;

/// ## ?
/// Instead of joining a lobby,
/// this is used to spectate a lobby instead.
/// The client will not shown in player list.
pub struct SpectateGameRequest {}
